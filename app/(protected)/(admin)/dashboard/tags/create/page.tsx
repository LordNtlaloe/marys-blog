// app/(protected)/(admin)/dashboard/tags/create/page.tsx

"use client"

import TagForm from "@/components/dashboard/tags/tags-form" // move TagForm to a separate file if not already

export default function CreateTagPage() {
  return <TagForm />
}
